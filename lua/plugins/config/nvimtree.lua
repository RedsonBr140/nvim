require('nvim-tree').setup {
    open_on_setup = true,
    renderer = {
        indent_markers = {
            enable = true
        },
        icons = {
            show = { git = false }
        }
    }
}
